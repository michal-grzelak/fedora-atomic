---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  # amd packages
  - from-file: hardware/amd-gpu.yml

  # canon printer drivers
  - from-file: hardware/canon-printer.yml

  # dev packages
  - from-file: common/dev-base.yml

  # gaming packages
  - from-file: common/gaming-bazzite.yml

  # install common base modules (system config, fonts)
  - from-file: common/base.yml

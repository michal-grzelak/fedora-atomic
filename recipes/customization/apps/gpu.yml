---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: default-flatpaks
    configurations:
      - notify: false
        scope: system
        repo:
          title: Flathub (system)
        install:
          # LACT - configure and monitor AMD, Nvidia or Intel GPU
          - io.github.ilya_zlobintsev.LACT
